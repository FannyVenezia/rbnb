<%= render "shared/navbar" %>

<div class="container_new">
  <div class="container-new-edit color">
    <div style="text-align: center">
      <h2>We hope you loved your experience</h2>
      <h3>share it with us !</h3>
    </div>

  <%= simple_form_for([@planet, @review]) do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.input :rating  %>
    <%= f.submit %>
  <% end %>
</div>
